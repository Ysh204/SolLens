[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
parser = { path = "../parser" }
runtime = { path = "../runtime" }
rust_decimal = { version = "1.34", features = ["serde-str"] }
serde = { version = "1.0", features = ["derive"] }
hex = "0.4"
